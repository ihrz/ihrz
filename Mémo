OUBLIE SA EN BAS
/*
await client.database.set(`BACKUPS.test`, { database: "test"});
console.log(await client.database.get(`BACKUPS`));

----------------------------------
const DataBaseModel = require(`${process.cwd()}/files/ihorizon-api/main.js`);
async function __All__() {
    return await DataBaseModel({ id: DataBaseModel.All });
}
(async () => {
    let e = await __All__();
    console.log(e);
})();
*/
OUBLIE SA EN HAUT

---------------------------------------------------------------
const DataBaseModel = require(`${process.cwd()}/files/ihorizon-api/main.js`);
await DataBaseModel({ id: DataBaseModel.Get, key: `${member.guild.id}.GUILD.GUILD_CONFIG.joindm` });
---------------------------------------------------------------

---------------------------------------------------------------
.Set
.Push
.Sub
.Add
.Get
.Pull
.All
.Delete
---------------------------------------------------------------

---------------------------------------------------------------
key: `PATH`, value/values: <value>
---------------------------------------------------------------